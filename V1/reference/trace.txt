In priority order — the first three determine the spec's entire shape:

app/bqs/ontology/ecm_dcm.yaml — the whole file. I've seen the header only. I need metrics, dimensions, filters, computed_filters, examples, and crucially how the physical object and any grain/dedupe are declared.
app/bqs/models.py — the BQS request/response contract (what the agent may send, what shapes come back).
app/bqs/sql_builder.py — how a request becomes SQL. Specifically: does it emit DISTINCT/GROUP BY automatically, and does it know about grain at all?
app/bqs/planner.py — how metrics/dimensions/filters resolve into a plan (and whether a plan can span more than one physical object).
app/bqs/entity/zen_entity_search.py — how names resolve today; this is where an entity dimension would plug in.
adk/config/tools.yaml — the exact signatures of discover_business_terms and run_bqs_query.
Optional but useful: sql_validator.py (does it carry rules like our 55?) and formatter.py (does it own presentation, or does the agent?).

grep -rn "requires_filters\|suggestable\|list_count" app/ --include=*.py
discover_business_terms